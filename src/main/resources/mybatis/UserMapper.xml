<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.minigame.mapper.UserMapper">
    <select id="selectByUsername" parameterType="String" resultType="com.minigame.entity.User">
        SELECT id, username, password, create_time AS createTime FROM user WHERE username = #{username}
    </select>

    <!-- 在UserMapper.xml中新增 -->
    <select id="selectByUsernameAndPassword" parameterType="map" resultType="com.minigame.entity.User">
        SELECT id, username, password FROM user
        WHERE username = #{username} AND password = #{password}
    </select>

    <insert id="insert" parameterType="com.minigame.entity.User" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO user (username, password, create_time) VALUES (#{username}, #{password}, #{createTime})
    </insert>
</mapper>
